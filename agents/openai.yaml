interface:
  display_name: "Deck Gen Pro"
  short_description: "Staged professional deck workflow"
  default_prompt: "Use $deck-gen-pro to start in plan mode, then turn my topic into a professional deck through staged research, style, outline, and build approvals."

policy:
  allow_implicit_invocation: true
